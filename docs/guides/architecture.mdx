---
title: Software factory architecture
description: Stable runtime, isolated sources, provider-owned adaptations, and deterministic output.
sidebar:
  icon: network
  order: 2
---

## Stable boundary

The shared `distilled` package owns the provider-neutral Effect engine, HTTP traits, and generator. Each SDK owns the Protocol layer that resolves credentials and implements its wire and error formats.

:::info
The accepted [Protocol architecture decision](/guides/protocol-architecture) makes this boundary explicit. Generated operations are uniform; provider behavior remains isolated and testable.
:::

<TypeTable
  type={{
    shared: {
      type: "distilled",
      required: true,
      description: "Protocol engine, common traits, pagination, retry, and generators.",
    },
    source: {
      type: "distilled-spec-* | upstream submodule",
      required: true,
      description: "An exact, reviewable pin of the authoritative API input.",
    },
    provider: {
      type: "distilled-<provider>",
      required: true,
      description: "Credentials, Protocol, patches, errors, retry policy, and generated services.",
    },
  }}
/>

## Repository topology

<Tree>
  <Tree.Folder name="distilled" defaultOpen>
    <Tree.File name="shared Effect Protocol engine" />
    <Tree.File name="OpenAPI generator" />
  </Tree.Folder>
  <Tree.Folder name="distilled-spec-jira" defaultOpen>
    <Tree.File name="official Atlassian OpenAPI" />
    <Tree.Folder name="distilled-jira">
      <Tree.File name="Protocol + patches + generated services" />
    </Tree.Folder>
  </Tree.Folder>
  <Tree.Folder name="distilled-spec-github" defaultOpen>
    <Tree.File name="versioned GitHub REST bundle" />
    <Tree.Folder name="distilled-github">
      <Tree.File name="Protocol + patches + generated services" />
    </Tree.Folder>
  </Tree.Folder>
  <Tree.Folder name="distilled-slack" defaultOpen>
    <Tree.File name="pinned slackapi/node-slack-sdk" />
    <Tree.File name="TypeScript AST generator" />
  </Tree.Folder>
  <Tree.Folder name="distilled-spec-avalara">
    <Tree.File name="official AvaTax Swagger" />
    <Tree.Folder name="distilled-avalara">
      <Tree.File name="provider Protocol + generated services" />
    </Tree.Folder>
  </Tree.Folder>
  <Tree.Folder name="distilled-spec-statsig">
    <Tree.File name="official Console API OpenAPI" />
    <Tree.Folder name="distilled-statsig">
      <Tree.File name="provider Protocol + generated services" />
    </Tree.Folder>
  </Tree.Folder>
  <Tree.Folder name="distilled-spec-auth0">
    <Tree.File name="official Management API OpenAPI" />
    <Tree.Folder name="distilled-auth0">
      <Tree.File name="provider Protocol + generated services" />
    </Tree.Folder>
  </Tree.Folder>
  <Tree.Folder name="distilled-spec-opensearch">
    <Tree.File name="official merged OpenAPI 3.1 release" />
    <Tree.Folder name="distilled-opensearch">
      <Tree.File name="cluster Protocol + generated services" />
    </Tree.Folder>
  </Tree.Folder>
</Tree>

## Source decision rule

| Upstream source | Repository strategy | Generator |
| --- | --- | --- |
| Maintained, compact OpenAPI | Mirror the authoritative document | Shared OpenAPI generator |
| Maintained OpenAPI in a very large repository | Mirror only the exact bundled document | Shared OpenAPI generator |
| No maintained OpenAPI; official typed SDK exists | Pin the official SDK directly | Provider-specific TypeScript AST generator |

:::warning
Do not scrape prose documentation into a pseudo-spec when a maintained typed source exists. The factory should preserve authority, not manufacture it.
:::

## Update flow

```mermaid
sequenceDiagram
  participant U as Upstream
  participant S as Source repository
  participant P as Provider SDK
  participant N as npm
  U->>S: publish source revision
  S->>P: advance exact submodule pin
  P->>P: apply patches and regenerate services
  P->>P: review committed diff
  P->>N: publish with GitHub OIDC
```

Generated output is committed. That is a feature: changes to upstream sources, generator behavior, and patches become ordinary reviewable diffs.
