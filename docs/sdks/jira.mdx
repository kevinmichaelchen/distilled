---
title: Jira SDK
description: Install, authenticate, and search Jira Cloud with Effect-native operations generated from Atlassian OpenAPI.
sidebar:
  icon: ticket-check
  badge: 0.1.2
  order: 3
---

<Badge variant="success">Public package</Badge> <Badge>OpenAPI</Badge> <Badge>587 operations</Badge>

The Jira SDK turns Atlassian's authoritative OpenAPI document into yieldable Effect operations with validated responses, typed failures, tracing, and retry policy.

## Install

<CodeGroup>

```bash npm
npm install @kevinmichaelchen/distilled-jira effect
```

```bash pnpm
pnpm add @kevinmichaelchen/distilled-jira effect
```

```bash bun
bun add @kevinmichaelchen/distilled-jira effect
```

</CodeGroup>

## Authentication

| Variable | Purpose |
| --- | --- |
| `JIRA_BASE_URL` | Jira Cloud site, such as `https://acme.atlassian.net` |
| `JIRA_EMAIL` | Atlassian account email |
| `JIRA_API_TOKEN` | Atlassian API token |

## Quickstart

Search for the highest-priority unresolved issues in a project:

```ts
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as FetchHttpClient from "effect/unstable/http/FetchHttpClient";
import {
  CredentialsFromEnv,
  searchAndReconcileIssuesUsingJql,
} from "@kevinmichaelchen/distilled-jira";

const JiraLive = Layer.mergeAll(FetchHttpClient.layer, CredentialsFromEnv);

const program = searchAndReconcileIssuesUsingJql({
  jql: 'project = "PAY" AND resolution = Unresolved ORDER BY priority DESC',
  fields: ["summary", "status", "priority", "assignee"],
  maxResults: 50,
});

const issues = await Effect.runPromise(Effect.provide(program, JiraLive));
```

:::tip[One layer, the whole Jira API]
The same layer can create and transition issues, add comments, manage projects and workflows, inspect permissions, administer fields, and automate users, filters, and dashboards. [See three common Jira workflows](/blog/announcing-distilled-jira#code-examples).
:::

## Source and package

<CardGroup cols={3}>
  <Card title="npm package" href="https://www.npmjs.com/package/@kevinmichaelchen/distilled-jira" icon="package-open">
    Public package, generated source, declarations, and release history.
  </Card>
  <Card title="Implementation (private)" href="https://github.com/kevinmichaelchen/distilled-jira" icon="lock-keyhole">
    Credentials, patches, errors, retries, tests, and generated operations.
  </Card>
  <Card title="Specification (private)" href="https://github.com/kevinmichaelchen/distilled-spec-jira" icon="lock-keyhole">
    Immutable mirror of Atlassian's official OpenAPI source.
  </Card>
</CardGroup>

Atlassian's small Java-style dotted operation-ID subset is normalized alongside its ordinary camelCase IDs, producing lower camelCase exports throughout.
