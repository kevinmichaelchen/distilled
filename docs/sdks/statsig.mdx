---
title: Statsig SDK
description: Install, authenticate, and manage Statsig resources with Effect-native Console API operations.
sidebar:
  icon: flag
  badge: 0.2.0
  order: 7
---

<Badge variant="success">Public package</Badge> <Badge>OpenAPI 3</Badge> <Badge>312 operations</Badge>

The Statsig SDK pins the official `20240601` Console API contract and generates the complete administrative surface through the shared OpenAPI path.

## Install

<CodeGroup>

```bash npm
npm install @kevinmichaelchen/distilled-statsig effect
```

```bash pnpm
pnpm add @kevinmichaelchen/distilled-statsig effect
```

```bash bun
bun add @kevinmichaelchen/distilled-statsig effect
```

</CodeGroup>

## Authentication

Set `STATSIG_API_KEY`. The provider Protocol sends it as `STATSIG-API-KEY` and pins the matching `STATSIG-API-VERSION` header.

## Quickstart

List active temporary feature gates:

```ts
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as FetchHttpClient from "effect/unstable/http/FetchHttpClient";
import {
  CredentialsFromEnv,
  Services,
} from "@kevinmichaelchen/distilled-statsig";

const StatsigLive = Layer.mergeAll(FetchHttpClient.layer, CredentialsFromEnv);

const program = Services.gates.gatesListGates({
  type: "TEMPORARY",
  includeArchived: "false",
  limit: 100,
});

const gates = await Effect.runPromise(Effect.provide(program, StatsigLive));
```

:::tip[One layer, the whole Statsig Console API]
The same layer can manage experiments, metrics, segments, layers, overrides, reviews, teams, environments, and release pipelines. [See three common Statsig workflows](/blog/announcing-distilled-statsig#code-examples).
:::

## Source and package

<CardGroup cols={3}>
  <Card title="npm package" href="https://www.npmjs.com/package/@kevinmichaelchen/distilled-statsig" icon="package-open">
    Public package, generated source, declarations, and release history.
  </Card>
  <Card title="Implementation (private)" href="https://github.com/kevinmichaelchen/distilled-statsig" icon="lock-keyhole">
    Statsig Protocol and 312 committed operations grouped by resource.
  </Card>
  <Card title="Specification (private)" href="https://github.com/kevinmichaelchen/distilled-spec-statsig" icon="lock-keyhole">
    Immutable mirror of Statsig's versioned Console API contract.
  </Card>
</CardGroup>

Statsig omits `operationId` throughout the source. Distilled derives stable lower camelCase names from tags and summaries and rejects collisions at the shared generator boundary.
